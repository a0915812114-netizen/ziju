# 擇一即可。有 Groq 會優先使用（較快、較便宜）。
GROQ_API_KEY=
OPENAI_API_KEY=

# 主人解鎖密語。不要放進網址列，請打開 /unlock 輸入。
OWNER_TOKEN=

# 可選。簽額度 cookie 用。沒填會改用上面的金鑰。
QUOTA_SECRET=

# 正式站額度跨機器共用。本機可不填。
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
